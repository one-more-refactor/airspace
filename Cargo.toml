[package]
name = "earshot"
version = "0.1.0-alpha"
edition = "2021"
rust-version = "1.80"
description = "A passive radio observatory: what your devices, and everyone else's, say out loud within earshot."
license = "GPL-3.0-or-later"
repository = "https://github.com/one-more-refactor/earshot"
readme = "README.md"
keywords = ["bluetooth", "ble", "privacy", "wireless", "sensing"]
categories = ["command-line-utilities"]

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "signal", "fs", "io-util"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zbus = { version = "5", default-features = false, features = ["tokio"] }
anyhow = "1"

[profile.release]
lto = true
codegen-units = 1
strip = true

[[bin]]
name = "earshot"
path = "src/main.rs"
